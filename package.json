{
  "name": "github-maven",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "node scripts/generate-tree.mjs && astro dev",
    "start": "astro dev",
    "build": "node scripts/generate-tree.mjs && astro check && astro build",
    "preview": "astro preview",
    "astro": "astro",
    "gen-tree": "node scripts/generate-tree.mjs"
  },
  "dependencies": {
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.0",
    "typescript": "^5.6.0"
  }
}
